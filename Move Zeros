class Solution(object):
    def moveZeroes(self, nums):
        p=0     
        for i in range(0,len(nums)):
            if nums[i]!=0:
                nums[p],nums[i]=nums[i],nums[p]
                p+=1
